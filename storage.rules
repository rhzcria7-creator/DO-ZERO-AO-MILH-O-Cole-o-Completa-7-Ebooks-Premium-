rules_version = '2';

// SECURITY AUDIT APPLIED
// Default Deny All Strategy for Storage
// Apenas o backend tem acesso ao PDF, servindo pela API Node.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
